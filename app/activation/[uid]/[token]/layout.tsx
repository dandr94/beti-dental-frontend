import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
    title: "Activating... | Beti Dental",
    description: "Beti Dental activation page",
};

export default function Layout() {
    return <Page />;
}